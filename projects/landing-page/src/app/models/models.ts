export interface MboxData {
  ownerId: string; // OwnerId pour identifier le joueur
  twoLetterISOLanguageName: string; // Langue du joueur
  casinoCurrencySymbol: string; // Symbole de devise ('€')
  egmCode: string; // CommandConfigSMBDID
  casinoId: string; // ID du casino
}
